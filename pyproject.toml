[project]
name = "fedpulse"
version = "0.3.0"
description = "Evidence-ranked federal regulatory monitoring from Federal Register and GPO metadata."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
