[project]
name = "fedpulse"
version = "0.1.0"
description = "Regulatory intelligence from GPO MARC + Federal Register metadata. Pure counts, no NLP."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
